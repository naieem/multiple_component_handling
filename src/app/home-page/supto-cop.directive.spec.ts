import { SuptoCopDirective } from './supto-cop.directive';

describe('SuptoCopDirective', () => {
  it('should create an instance', () => {
    const directive = new SuptoCopDirective();
    expect(directive).toBeTruthy();
  });
});
